import { ref } from 'vue'
// import { WeeklyReportContent } from '@/stores/report'

// const currentReport = ref<WeeklyReportContent>({ content: '' })

// currentReport.value.content = '本周工作总结...'


// declare module 'mammoth'

// ElMessageBox.confirm('确定要退出登录吗？')

// if (requiredPermissions && requiredPermissions.length > 0) { ... }